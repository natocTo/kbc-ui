import React from 'react';

export default () => {
  return React.createClass({
    render: function() {
      return (
        <span>
          Configuration migrated successfully.
        </span>
      );
    }
  });
};
